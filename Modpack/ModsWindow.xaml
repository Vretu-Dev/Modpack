<Window x:Class="WotModpackLoader.ModsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz mody do instalacji"
        Height="400" Width="550"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="10" ShadowDepth="3" Direction="270" Color="#22000000" />
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Background" Value="#FF1A73E8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF185ABC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Effect="{StaticResource ShadowEffect}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
        </Style>
        <!-- Styl dla zaokrąglonego ProgressBar -->
        <Style x:Key="RoundedProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Foreground" Value="#FF1A73E8"/>
            <Setter Property="Background" Value="#E8F0FE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                SnapsToDevicePixels="true"
                                Height="{TemplateBinding Height}">
                            <Grid ClipToBounds="True">
                                <Rectangle x:Name="PART_Track" 
                                           Fill="{TemplateBinding Background}" 
                                           RadiusX="10" RadiusY="10"
                                           Height="{TemplateBinding Height}" />
                                <Rectangle x:Name="PART_Indicator"
                                           Fill="{TemplateBinding Foreground}" 
                                           RadiusX="10" RadiusY="10"
                                           Height="{TemplateBinding Height}" 
                                           HorizontalAlignment="Left" />
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="PART_Indicator" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border CornerRadius="14" Padding="16" Margin="14" Background="White" Effect="{StaticResource ShadowEffect}">
        <StackPanel>
            <TextBlock Text="Wybierz mody do zainstalowania:" FontSize="18" FontWeight="Bold" Foreground="#185ABC" Margin="0,0,0,14"/>
            <CheckBox Content="XVM - Rozbudowany interfejs (Wymagany)" Name="XvmCheck" IsChecked="True"/>
            <CheckBox Content="PMOD (Wskaźnik penetracji)" Name="PmodCheck" IsChecked="True"/>
            <CheckBox Content="Battle Equipment (widoczność wyposażenia w czasie bitwy)" Name="BattleEquipmentCheck" IsChecked="True"/>
            <CheckBox Content="Odtwarzacz powtórek" Name="ReplaysCheck" IsChecked="True"/>
            <ProgressBar x:Name="InstallProgressBar"
                         Style="{StaticResource RoundedProgressBar}"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         Margin="0,10,0,0"
                         Visibility="Collapsed"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                <Button Content="Powróć" Width="120" Height="38" Margin="0,0,16,0" Click="BackButton_Click"/>
                <Button Content="Zainstaluj wybrane mody" Width="220" Height="38" Click="InstallButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>